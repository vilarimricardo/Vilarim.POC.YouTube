#EXECUTE

Para executar os dois projetos execute o ./show.sh

#Não implementadas

- Implementação de serviço de log
- Persistencia IMemory para testes
- Persistencia Postgres para prod
- Publicação Aws (problemas com configuração de HTTPS no docker)

#Requisito em dúvida

- Exibir dados do cache + aos do retorno da Api do Google (Merge de resultas)?


